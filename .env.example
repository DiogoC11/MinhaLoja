# SMTP configuration (production)
# On Vercel, configure these in Project Settings > Environment Variables
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Minha Loja <no-reply@exemplo.com>"

# Public site URL (optional, if you need absolute URLs in emails)
NEXT_PUBLIC_SITE_URL=